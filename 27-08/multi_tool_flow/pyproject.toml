[project]
name = "multi_tool_flow"
version = "0.1.0"
description = "multi_tool_flow using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=0.165.1,<1.0.0",
    "ddgs>=9.5.4",
]

[project.scripts]
kickoff = "multi_tool_flow.main:kickoff"
run_crew = "multi_tool_flow.main:kickoff"
plot = "multi_tool_flow.main:plot"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
